# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# Env
.env
.env.*
.envrc
!.env.example
backend/.env
frontend/.env

# Production infra config — lives only on the VPS, never in this public repo
docker-compose.prod.yml
docker-compose.prod.yml.*

# IDE
.idea/
.vscode/

# Logs
*.log

# Credenciales ARCA
data/*
!data/.gitkeep
certificados_ws/*
certificados_clientes/*
certificados_clientes/**
!certificados_clientes/.gitkeep
*.pdf

# Local notes/artifacts
doc_generacion_key_csr.md
*.pen
ui_clientes_crud.md
manual_wslpg_*.txt
ejemplo_playwright.txt
respuestas_sincronizacion_rpa.md
scripts/diag_coe_estado.py

# Agent/tooling artifacts — not part of the project
.atl/
.playwright-mcp/
vps-*.png

# Infra runbook (lives only in /opt/apps/granos/ on the VPS and in the
# operator's local copy — not in the repo, to avoid leaking the VPS layout).
INFRA.md
